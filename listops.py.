# Define the list of numbers

numbers = [1, 2, 3, 13, 5, 6, 7, 8, 9, 10]

# Calculate the total sum of the numbers in the list

total_sum = sum(numbers)

# Determine the largest number in the list

largest_number = max(numbers)

# Determine the smallest number in the list

smallest_number = min(numbers)

# Reverse the order of the list

reversed_list = numbers[::-1]

# Display the results

print("Original list:", numbers)

print("Total sum of the numbers:", total_sum)

print("Largest number:", largest_number)

print("Smallest number:", smallest_number)

print("Reversed list:", reversed_list) 
